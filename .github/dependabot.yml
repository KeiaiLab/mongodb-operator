version: 2

updates:
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "UTC"
    open-pull-requests-limit: 10
    target-branch: "main"
    labels:
      - "dependencies"
      - "go"
    commit-message:
      prefix: "chore(deps)"
      prefix-development: "chore(deps-dev)"
      include: "scope"
    groups:
      kubernetes-dependencies:
        patterns:
          - "k8s.io/*"
          - "sigs.k8s.io/*"
        exclude-patterns:
          - "*test*"
      controller-runtime:
        patterns:
          - "sigs.k8s.io/controller-runtime*"
      test-dependencies:
        patterns:
          - "github.com/onsi/ginkgo*"
          - "github.com/onsi/gomega*"
          - "github.com/stretchr/testify*"
        exclude-patterns:
          - "*"
    pull-request-branch-name:
      separator: "-"
    allow:
      - dependency-type: "direct"
      - dependency-type: "indirect"
    ignore:
      - dependency-name: "k8s.io/*"
        update-types: ["version-update:semver-major"]
      - dependency-name: "sigs.k8s.io/controller-runtime"
        update-types: ["version-update:semver-major"]
    rebase-strategy: "auto"
    # reviewers/assignees 비움: org 단독("keiailab")은 GitHub Dependabot에서 invalid이며
    # 유효 user/team이 정해지면 "keiailab/team-slug" 형태로 추가. 비우면 자동 할당 없음.
