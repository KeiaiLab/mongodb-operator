---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: mongodbclustergroups.mongodb.keiailab.com
spec:
  group: mongodb.keiailab.com
  names:
    kind: MongoDBClusterGroup
    listKind: MongoDBClusterGroupList
    plural: mongodbclustergroups
    shortNames:
    - mdbgroup
    singular: mongodbclustergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.members[*].name
      name: Members
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MongoDBClusterGroup is the Schema for the mongodbclustergroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoDBClusterGroupSpec — F56 (cycle 8).
            properties:
              centralMonitoring:
                description: |-
                  CentralMonitoring defines the central Grafana / Prometheus aggregation
                  destination (단일 control plane 의 통합 dashboard).
                properties:
                  alertmanagerURL:
                    description: AlertmanagerURL — 통합 alert sink.
                    type: string
                  grafanaURL:
                    description: GrafanaURL — central Grafana instance (정합 dashboard
                      설치 hint).
                    type: string
                  prometheusRemoteWriteURL:
                    description: |-
                      PrometheusRemoteWriteURL — 각 member 의 mongodb-exporter 가
                      remote write 로 metrics 를 보내는 endpoint.
                    type: string
                type: object
              members:
                description: Members lists the cluster targets controlled by this
                  group.
                items:
                  description: ClusterGroupMember — 한 cluster target.
                  properties:
                    clusterKubeConfigRef:
                      description: |-
                        ClusterKubeConfigRef references the kubeconfig Secret for cross-
                        cluster members. nil → same-cluster (operator 가 직접 reconcile).
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    kind:
                      default: MongoDB
                      description: Kind = MongoDB | MongoDBSharded — 어느 CRD 의 reference
                        인지.
                      enum:
                      - MongoDB
                      - MongoDBSharded
                      type: string
                    name:
                      description: Name human-readable identifier within the group.
                      minLength: 1
                      type: string
                    namespacedName:
                      description: |-
                        NamespacedName references the target CR (same-cluster scenario) or
                        the kubeconfig (cross-cluster scenario).
                      properties:
                        apiVersion:
                          description: API version of the referent.
                          type: string
                        fieldPath:
                          description: |-
                            If referring to a piece of an object instead of an entire object, this string
                            should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                            For example, if the object reference is to a container within a pod, this would take on a value like:
                            "spec.containers{name}" (where "name" refers to the name of the container that triggered
                            the event) or if no container name is specified "spec.containers[2]" (container with
                            index 2 in this pod). This syntax is chosen only to have some well-defined way of
                            referencing a part of an object.
                          type: string
                        kind:
                          description: |-
                            Kind of the referent.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                          type: string
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        namespace:
                          description: |-
                            Namespace of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                          type: string
                        resourceVersion:
                          description: |-
                            Specific resourceVersion to which this reference is made, if any.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                          type: string
                        uid:
                          description: |-
                            UID of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                  - name
                  - namespacedName
                  type: object
                minItems: 1
                type: array
              policyTemplate:
                description: |-
                  PolicyTemplate — 모든 member 에 강제 적용할 정책 (예: backup
                  frequency, retention 최소값). 각 member 가 override 가능하지만 본
                  template 보다 *약한* 정책은 webhook 에서 reject.
                properties:
                  minBackupRetentionDays:
                    default: 7
                    description: |-
                      MinBackupRetentionDays — member 의 BackupSpec.Retention.Days 가 이
                      값 이상이어야 함. 작은 값으로 설정 시 webhook reject.
                    format: int32
                    type: integer
                  requiredEncryptionAtRest:
                    default: false
                    description: RequiredEncryptionAtRest — 모든 member 가 encryption-at-rest
                      필수.
                    type: boolean
                  requiredTLSEnabled:
                    default: false
                    description: RequiredTLSEnabled — 모든 member 가 TLS 활성 필수.
                    type: boolean
                type: object
              sharedAuth:
                description: |-
                  SharedAuth defines the *global user* policy applied to all members.
                  전역 사용자가 각 cluster 에 동일 username / role / password secret 으로
                  자동 등록.
                properties:
                  users:
                    description: Users 가 각 member 에 reconcile 되는 user 목록.
                    items:
                      description: MongoDBUser defines a MongoDB user
                      properties:
                        db:
                          description: DB is the authentication database
                          type: string
                        name:
                          description: Name is the username
                          type: string
                        passwordSecretRef:
                          description: PasswordSecretRef references the password secret
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        roles:
                          description: Roles defines user roles
                          items:
                            description: MongoDBRole defines a MongoDB role
                            properties:
                              db:
                                description: DB is the database for the role
                                type: string
                              name:
                                description: Name is the role name
                                type: string
                            required:
                            - db
                            - name
                            type: object
                          type: array
                      required:
                      - db
                      - name
                      - passwordSecretRef
                      - roles
                      type: object
                    type: array
                type: object
            required:
            - members
            type: object
          status:
            description: MongoDBClusterGroupStatus — group 단위 status.
            properties:
              conditions:
                description: Conditions follow standard k8s convention.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              memberStatuses:
                description: MemberStatuses 각 member 의 sync 결과.
                items:
                  description: ClusterGroupMemberStatus — per-member 상태.
                  properties:
                    error:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    policyCompliant:
                      description: PolicyCompliant — member 가 policyTemplate 을 모두
                        만족하는지.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
              phase:
                description: 'Phase: Pending | Reconciling | Synced | Degraded'
                enum:
                - Pending
                - Reconciling
                - Synced
                - Degraded
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.members[*].name
      name: Members
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MongoDBClusterGroup is the Schema for the mongodbclustergroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoDBClusterGroupSpec — F56 (cycle 8).
            properties:
              centralMonitoring:
                description: |-
                  CentralMonitoring defines the central Grafana / Prometheus aggregation
                  destination (단일 control plane 의 통합 dashboard).
                properties:
                  alertmanagerURL:
                    description: AlertmanagerURL — 통합 alert sink.
                    type: string
                  grafanaURL:
                    description: GrafanaURL — central Grafana instance (정합 dashboard
                      설치 hint).
                    type: string
                  prometheusRemoteWriteURL:
                    description: |-
                      PrometheusRemoteWriteURL — 각 member 의 mongodb-exporter 가
                      remote write 로 metrics 를 보내는 endpoint.
                    type: string
                type: object
              members:
                description: Members lists the cluster targets controlled by this
                  group.
                items:
                  description: ClusterGroupMember — 한 cluster target.
                  properties:
                    clusterKubeConfigRef:
                      description: |-
                        ClusterKubeConfigRef references the kubeconfig Secret for cross-
                        cluster members. nil → same-cluster (operator 가 직접 reconcile).
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    kind:
                      default: MongoDB
                      description: Kind = MongoDB | MongoDBSharded — 어느 CRD 의 reference
                        인지.
                      enum:
                      - MongoDB
                      - MongoDBSharded
                      type: string
                    name:
                      description: Name human-readable identifier within the group.
                      minLength: 1
                      type: string
                    namespacedName:
                      description: |-
                        NamespacedName references the target CR (same-cluster scenario) or
                        the kubeconfig (cross-cluster scenario).
                      properties:
                        apiVersion:
                          description: API version of the referent.
                          type: string
                        fieldPath:
                          description: |-
                            If referring to a piece of an object instead of an entire object, this string
                            should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                            For example, if the object reference is to a container within a pod, this would take on a value like:
                            "spec.containers{name}" (where "name" refers to the name of the container that triggered
                            the event) or if no container name is specified "spec.containers[2]" (container with
                            index 2 in this pod). This syntax is chosen only to have some well-defined way of
                            referencing a part of an object.
                          type: string
                        kind:
                          description: |-
                            Kind of the referent.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                          type: string
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        namespace:
                          description: |-
                            Namespace of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                          type: string
                        resourceVersion:
                          description: |-
                            Specific resourceVersion to which this reference is made, if any.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                          type: string
                        uid:
                          description: |-
                            UID of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                  - name
                  - namespacedName
                  type: object
                minItems: 1
                type: array
              policyTemplate:
                description: |-
                  PolicyTemplate — 모든 member 에 강제 적용할 정책 (예: backup
                  frequency, retention 최소값). 각 member 가 override 가능하지만 본
                  template 보다 *약한* 정책은 webhook 에서 reject.
                properties:
                  minBackupRetentionDays:
                    default: 7
                    description: |-
                      MinBackupRetentionDays — member 의 BackupSpec.Retention.Days 가 이
                      값 이상이어야 함. 작은 값으로 설정 시 webhook reject.
                    format: int32
                    type: integer
                  requiredEncryptionAtRest:
                    default: false
                    description: RequiredEncryptionAtRest — 모든 member 가 encryption-at-rest
                      필수.
                    type: boolean
                  requiredTLSEnabled:
                    default: false
                    description: RequiredTLSEnabled — 모든 member 가 TLS 활성 필수.
                    type: boolean
                type: object
              sharedAuth:
                description: |-
                  SharedAuth defines the *global user* policy applied to all members.
                  전역 사용자가 각 cluster 에 동일 username / role / password secret 으로
                  자동 등록.
                properties:
                  users:
                    description: Users 가 각 member 에 reconcile 되는 user 목록.
                    items:
                      description: MongoDBUser defines a MongoDB user
                      properties:
                        db:
                          description: DB is the authentication database
                          type: string
                        name:
                          description: Name is the username
                          type: string
                        passwordSecretRef:
                          description: PasswordSecretRef references the password secret
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        roles:
                          description: Roles defines user roles
                          items:
                            description: MongoDBRole defines a MongoDB role
                            properties:
                              db:
                                description: DB is the database for the role
                                type: string
                              name:
                                description: Name is the role name
                                type: string
                            required:
                            - db
                            - name
                            type: object
                          type: array
                      required:
                      - db
                      - name
                      - passwordSecretRef
                      - roles
                      type: object
                    type: array
                type: object
            required:
            - members
            type: object
          status:
            description: MongoDBClusterGroupStatus — group 단위 status.
            properties:
              conditions:
                description: Conditions follow standard k8s convention.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              memberStatuses:
                description: MemberStatuses 각 member 의 sync 결과.
                items:
                  description: ClusterGroupMemberStatus — per-member 상태.
                  properties:
                    error:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    policyCompliant:
                      description: PolicyCompliant — member 가 policyTemplate 을 모두
                        만족하는지.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
              phase:
                description: 'Phase: Pending | Reconciling | Synced | Degraded'
                enum:
                - Pending
                - Reconciling
                - Synced
                - Degraded
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
