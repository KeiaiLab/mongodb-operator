---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: mongodbinsights.mongodb.keiailab.com
spec:
  group: mongodb.keiailab.com
  names:
    kind: MongoDBInsights
    listKind: MongoDBInsightsList
    plural: mongodbinsights
    shortNames:
    - mdbinsights
    singular: mongodbinsights
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.recommendations[*].type
      name: Recommendations
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MongoDBInsights is the Schema for advisory analysis.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoDBInsightsSpec — F51 (cycle 7) 분석 정책.
            properties:
              analysisCredentialsSecretRef:
                description: |-
                  AnalysisCredentialsSecretRef — system.profile 읽기 권한 user.
                  미설정 시 cluster 의 admin credentials 재사용.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              analysisInterval:
                default: 15m
                description: AnalysisInterval — reconcile 간격.
                type: string
              clusterRef:
                description: ClusterRef references the target MongoDB or MongoDBSharded.
                properties:
                  kind:
                    description: Kind is the cluster kind (MongoDB or MongoDBSharded)
                    enum:
                    - MongoDB
                    - MongoDBSharded
                    type: string
                  name:
                    description: Name is the cluster name
                    type: string
                required:
                - kind
                - name
                type: object
              enabled:
                default: true
                description: Enabled toggles the insights collector. 기본 true.
                type: boolean
              profilingLevel:
                default: 1
                description: ProfilingLevel = 0 (off) | 1 (slow only) | 2 (all). 기본
                  1.
                enum:
                - 0
                - 1
                - 2
                format: int32
                type: integer
              sampleSize:
                default: 500
                description: SampleSize — 각 reconcile cycle 당 분석할 system.profile 문서
                  수.
                format: int32
                type: integer
              slowQueryThresholdMs:
                default: 100
                description: SlowQueryThresholdMs — system.profile 에 기록될 최소 latency.
                format: int32
                type: integer
              targetKind:
                default: MongoDB
                description: |-
                  TargetKind 는 분석 대상 CRD 종류. MongoDB 또는 MongoDBSharded.
                  #226: MongoDBSharded 지원 — sharded cluster 의 각 shard + mongos 의
                  system.profile 을 수집하여 통합 분석.
                enum:
                - MongoDB
                - MongoDBSharded
                type: string
            required:
            - clusterRef
            type: object
          status:
            description: MongoDBInsightsStatus — 분석 결과.
            properties:
              conditions:
                description: Conditions follow standard k8s convention.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAnalysisTime:
                description: LastAnalysisTime 마지막 분석 완료 시점.
                format: date-time
                type: string
              phase:
                description: 'Phase: Pending | Analyzing | Ready | Failed'
                enum:
                - Pending
                - Analyzing
                - Ready
                - Failed
                type: string
              recommendations:
                description: Recommendations — 인덱스 추가 / schema 개선 / slow query 패턴.
                items:
                  description: Recommendation — 분석 결과 1건.
                  properties:
                    avgLatencyMs:
                      description: AvgLatencyMs 권장 근거 latency.
                      format: int32
                      type: integer
                    collection:
                      type: string
                    db:
                      description: DB + Collection (optional — schema-level hint 시
                        비어있음).
                      type: string
                    detail:
                      description: Detail human-readable 권장. e.g. "create compound
                        index on (status, createdAt)"
                      type: string
                    indexSuggestion:
                      description: IndexSuggestion BSON-style key spec (MissingIndex
                        시).
                      type: string
                    querySamples:
                      description: QuerySamples — sample query string (max 3).
                      items:
                        type: string
                      type: array
                    severity:
                      default: info
                      description: Severity = info | warning | critical
                      enum:
                      - info
                      - warning
                      - critical
                      type: string
                    type:
                      description: Type = MissingIndex | UnusedIndex | SlowQueryPattern
                        | SchemaHint
                      enum:
                      - MissingIndex
                      - UnusedIndex
                      - SlowQueryPattern
                      - SchemaHint
                      type: string
                  required:
                  - detail
                  - type
                  type: object
                type: array
              slowQueriesSampled:
                description: SlowQueriesSampled — 마지막 cycle 에 분석된 slow query 수.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.recommendations[*].type
      name: Recommendations
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MongoDBInsights is the Schema for advisory analysis.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoDBInsightsSpec — F51 (cycle 7) 분석 정책.
            properties:
              analysisCredentialsSecretRef:
                description: |-
                  AnalysisCredentialsSecretRef — system.profile 읽기 권한 user.
                  미설정 시 cluster 의 admin credentials 재사용.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              analysisInterval:
                default: 15m
                description: AnalysisInterval — reconcile 간격.
                type: string
              clusterRef:
                description: ClusterRef references the target MongoDB or MongoDBSharded.
                properties:
                  kind:
                    description: Kind is the cluster kind (MongoDB or MongoDBSharded)
                    enum:
                    - MongoDB
                    - MongoDBSharded
                    type: string
                  name:
                    description: Name is the cluster name
                    type: string
                required:
                - kind
                - name
                type: object
              enabled:
                default: true
                description: Enabled toggles the insights collector. 기본 true.
                type: boolean
              profilingLevel:
                default: 1
                description: ProfilingLevel = 0 (off) | 1 (slow only) | 2 (all). 기본
                  1.
                enum:
                - 0
                - 1
                - 2
                format: int32
                type: integer
              sampleSize:
                default: 500
                description: SampleSize — 각 reconcile cycle 당 분석할 system.profile 문서
                  수.
                format: int32
                type: integer
              slowQueryThresholdMs:
                default: 100
                description: SlowQueryThresholdMs — system.profile 에 기록될 최소 latency.
                format: int32
                type: integer
            required:
            - clusterRef
            type: object
          status:
            description: MongoDBInsightsStatus — 분석 결과.
            properties:
              conditions:
                description: Conditions follow standard k8s convention.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAnalysisTime:
                description: LastAnalysisTime 마지막 분석 완료 시점.
                format: date-time
                type: string
              phase:
                description: 'Phase: Pending | Analyzing | Ready | Failed'
                enum:
                - Pending
                - Analyzing
                - Ready
                - Failed
                type: string
              recommendations:
                description: Recommendations — 인덱스 추가 / schema 개선 / slow query 패턴.
                items:
                  description: Recommendation — 분석 결과 1건.
                  properties:
                    avgLatencyMs:
                      description: AvgLatencyMs 권장 근거 latency.
                      format: int32
                      type: integer
                    collection:
                      type: string
                    db:
                      description: DB + Collection (optional — schema-level hint 시
                        비어있음).
                      type: string
                    detail:
                      description: Detail human-readable 권장. e.g. "create compound
                        index on (status, createdAt)"
                      type: string
                    indexSuggestion:
                      description: IndexSuggestion BSON-style key spec (MissingIndex
                        시).
                      type: string
                    querySamples:
                      description: QuerySamples — sample query string (max 3).
                      items:
                        type: string
                      type: array
                    severity:
                      default: info
                      description: Severity = info | warning | critical
                      enum:
                      - info
                      - warning
                      - critical
                      type: string
                    type:
                      description: Type = MissingIndex | UnusedIndex | SlowQueryPattern
                        | SchemaHint
                      enum:
                      - MissingIndex
                      - UnusedIndex
                      - SlowQueryPattern
                      - SchemaHint
                      type: string
                  required:
                  - detail
                  - type
                  type: object
                type: array
              slowQueriesSampled:
                description: SlowQueriesSampled — 마지막 cycle 에 분석된 slow query 수.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
