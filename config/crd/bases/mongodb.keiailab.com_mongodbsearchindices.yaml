---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: mongodbsearchindices.mongodb.keiailab.com
spec:
  group: mongodb.keiailab.com
  names:
    kind: MongoDBSearchIndex
    listKind: MongoDBSearchIndexList
    plural: mongodbsearchindices
    shortNames:
    - mdbsearchidx
    singular: mongodbsearchindex
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.searchRef.name
      name: Search
      type: string
    - jsonPath: .spec.collection
      name: Collection
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MongoDBSearchIndex is the Schema for declarative $search / $vectorSearch
          indexes.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoDBSearchIndexSpec — 한 컬렉션의 search/vector 인덱스 1개.
            properties:
              collection:
                description: Collection 대상 컬렉션.
                minLength: 1
                type: string
              database:
                description: Database 대상 database.
                minLength: 1
                type: string
              definitionJSON:
                description: |-
                  DefinitionJSON — 인덱스 정의를 JSON 으로(createSearchIndex 의 definition).
                  search: {"mappings":{...}}. vectorSearch: {"fields":[{"type":"vector","path":..,"similarity":"cosine","numDimensions":1536}]}.
                  정의 유효성은 mongot 가 createSearchIndex 시 검증(VerificationQuery.FilterJSON 동일 raw-JSON 컨벤션).
                minLength: 2
                type: string
              indexName:
                description: IndexName — search index 이름($search/$vectorSearch 의 index
                  인자).
                minLength: 1
                type: string
              searchRef:
                description: SearchRef — 이 인덱스를 호스팅할 MongoDBSearch(mongot) 참조.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              type:
                default: search
                description: Type — search(full-text/Lucene) 또는 vectorSearch(AI 임베딩).
                enum:
                - search
                - vectorSearch
                type: string
            required:
            - collection
            - database
            - definitionJSON
            - indexName
            - searchRef
            type: object
          status:
            description: MongoDBSearchIndexStatus — 인덱스 빌드 상태.
            properties:
              conditions:
                description: Conditions follow standard k8s convention.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                description: Error — 실패 사유.
                type: string
              indexID:
                description: IndexID — mongot 가 할당한 search index id(존재 시).
                type: string
              observedGeneration:
                description: ObservedGeneration — 마지막 reconcile 한 spec generation.
                format: int64
                type: integer
              phase:
                description: 'Phase: Pending | Building | Ready | Failed | Deleting'
                enum:
                - Pending
                - Building
                - Ready
                - Failed
                - Deleting
                type: string
              queryable:
                description: Queryable — 인덱스가 쿼리 가능 상태($search/$vectorSearch 응답 가능).
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
