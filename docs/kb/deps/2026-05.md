# 의존성 변경 로그 — 2026-05 (mongodb-operator)

> 위치 표준: `<repo>/docs/kb/deps/YYYY-MM.md` (글로벌 `standards/enforcement.md §2.4`).
> 갱신 정책: lockfile (`go.mod` / `go.sum`) diff 가 발생하면 본 파일에 append.
>            자동화: lefthook/pre-commit `post-merge` 또는 `post-commit` hook 으로
>            lockfile diff 감지 시 PR 작성자가 본 파일에 1행 추가.

## 베이스라인 (2026-05-06 — 본 파일 seed)

- `go.mod` direct require: 10건
- `go.mod` 총 line (direct + indirect): 107건
- 핵심 direct dep:
  - controller-runtime / client-go (k8s.io)
  - mongo-go-driver
  - onsi/ginkgo + gomega (test)
  - stretchr/testify
  - prometheus/client_golang

본 파일 seed 시점에 미해결 의존성 변경 *없음*. 다음 lockfile diff 시 변경 항목을 표 형태로 append.

## 변경 로그 (append-only)

| 일자 | 변경 | 사유 | 검증 (audit) |
|------|------|------|--------------|
| 2026-05-06 | seed (no change) | enforcement §2.4 의 P1 추적성 도입 | n/a |
| 2026-05-09 | no change (audit cross-cut) | 4-repo 일관성 audit — 도구·문서·설정만 변경, go.mod 영향 0 | go.mod / go.sum diff 0 |

## 검증 명령

```bash
# direct dep diff
git diff -- go.mod go.sum | head -100

# 보안 audit (Makefile L3)
make audit          # govulncheck + trivy + gosec

# Sonatype Guide MCP 사전 검증 (신규/upgrade 의존성)
# CLAUDE.md §2 — 외부 라이브러리 사용 전 context7/sonatype-guide MCP 의무.
```

## 글로벌 참조

- 강제 표준: `~/Documents/ai-dev/standards/enforcement.md §2.4` (의존성 감사 로그)
- 사전 검증 SDK: `sonatype-guide:sonatype-guide` skill — 의존성 추가/업그레이드 전 의무 호출.
